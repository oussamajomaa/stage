{% extends 'base.html.twig' %}

{% block title %}Modifier un retard{% endblock %}

{% block body %}
{% if app.user %}


{# structure de la page 10 colonnes de largeur et alignée à droite #}
<div class="container-fluid">
    <div class="row">
        <div class="col-xl-10 col-lg-9 col-md-8 ml-auto mb-5 mt-5">
            {% include "editor/apprenant/_nav_apprenant.html.twig" with {"p": 2} %}
            
            
            <div class="my-5">
                <h4 class="btn btn-info btn-sm">Modifier un retard</h4>
                
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-outline-danger d-block ml-auto">Enregistrer</button>
                {{ form_end(form) }}

            </div>
            {# end body #}

        </div>
    </div>
</div>
{% endif %}
{% endblock %}

{% block javascripts %}

{# <script>
    let $token = $('#retard__token')
    let $retard_promotion = $('#retard_promotion')
    let $retard_date = $('#retard_date')

    $retard_promotion.change(function () {
        let $form = $(this).closest('form')
        let data = {}
        data[$token.attr('name')] = $token.val()
        data[$retard_promotion.attr('name')] = $retard_promotion.val()
        data[$retard_date.attr('name')] = $retard_date.val()


        $.post($form.attr('action'), data).then(function (data) {
            $('#retard_apprenant').replaceWith(
                $(data).find('#retard_apprenant')
            )
        })
    })

</script> #}

{% endblock %}